{
  "name": "server-survival",
  "version": "1.0.0",
  "private": true,
  "description": "Dev tooling for Server Survival. The game itself has NO build step — it is served raw from this repo by GitHub Pages. Everything here is optional and for contributors only (lint, tests, CI).",
  "scripts": {
    "lint": "eslint .",
    "test": "vitest run",
    "test:watch": "vitest",
    "check": "npm run lint && npm run test"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.7.0",
    "globals": "^17.7.0",
    "vitest": "^4.1.10"
  }
}
